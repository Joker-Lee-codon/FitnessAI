---
topic: FitnessAI implementation contract for vibe coding and human verification
updated: 2026-09-10T16:08
---

- (direction) Use the final 2026-09-10 Architecture Spine as the newest Product Owner authority when older PRD or UX wording conflicts; do not reopen confirmed product or architecture choices.
- (direction) Produce an English canonical implementation SPEC and structurally identical zh-CN copies suitable for vibe coding and human verification.
- (decision) The specification covers the personally usable core-product private Alpha; public-beta cloud continuity and post-Alpha expansion remain gated later scope.
- (decision) The contract is a lean kernel plus adopted PRD, UX, architecture, native UI, and design-system companions, with spec-authored implementation and verification companions.
- (decision) PRD and UX were upstream-aligned on 2026-09-10 for first-class phone-only capture and one-shot per-request remote use of selected local Apple Health summaries.
- (capability) CAP-1: A trainee can create or revise a complete structured Training Plan through text AI, review every field or diff, and activate exactly one immutable revision only after digest-bound explicit approval; success is zero silent or partial activation and one complete committed revision per valid approval.
- (capability) CAP-2: A trainee can execute a delivered Plan snapshot or ad-hoc workout on Apple Watch while facts save locally and remain usable offline; success is a complete or visibly incomplete session whose committed facts survive disconnect and recovery exactly once.
- (capability) CAP-3: A trainee without a Watch can run planned or ad-hoc sessions on iPhone using the same identities, states, corrections, and Analysis; success is parity of truth and recovery semantics without simulated Watch automation or a second domain model.
- (capability) CAP-4: Supported Watch configurations can assist Exercise classification, set boundaries, and rep counts with confidence, abstention, rapid correction, and manual fallback; success is independent qualification against frozen PRD gates while unsupported paths never claim success.
- (capability) CAP-5: A user who opts in can verify, correct, and later explicitly enable high-confidence RPE auto-use with provenance and drift recovery; success is separated predicted, confirmed, corrected, auto-used, and effective values with no recursive labels.
- (capability) CAP-6: Watch may offer a forgotten-Finish prompt only after the frozen dual-signal window and never end automatically; success is manual authority, missing-signal suppression, and correct cooldown under physical-device tests.
- (capability) CAP-7: Users can correct, split, merge, insert, delete, recover, transfer, and resolve conflicting workout facts without silent overwrite or duplication; success is append-only causal revisions with every user fact preserved until a safe effective revision exists.
- (capability) CAP-8: Users can inspect History and weekly, monthly, and selected-Exercise Analysis derived only from effective Actual Sets; success is current results or explicitly stale, pending, partial, or excluded data with no invented precision.
- (capability) CAP-9: Users can grant least-privilege Apple Health capabilities, maintain FitnessAI local authority, and optionally preview selected local summaries for separate remote authorization; success is observable read/write/detach states and zero remote Health transmission before the fail-closed gate.
- (capability) CAP-10: Users can export one bounded FitnessAI JSON Plan and import it only as a fully previewed independent draft; success is no hidden data, parser abuse, silent omission, merge, overwrite, or activation.
- (capability) CAP-11: A completed session may show one Review, Later, or Disable invitation that enters the existing authorization and proposal flow; success is no send, authorization, proposal, or approval merely from display or dismissal.
- (capability) CAP-12: Every Alpha surface implements the accepted native navigation, Titanium Measure semantics, recovery states, and accessibility contract; success is complete light/dark, Dynamic Type, VoiceOver, error, empty, loading, offline, and recovery behavior without placeholder screens.
- (capability) CAP-13: Users can inspect canonical Exercise support, mappings, body-area and movement attributes, and concise owned or licensed teaching text; success is explicit supported, limited, and manual-only status with no unlicensed media or wrist-only full-body claims.
- (constraint) Use one local-first hexagonal modular monolith repository/workspace with separate iPhone and Watch targets and one local Swift Package; domain depends on no UI, storage, device, Health, sensor, or AI-provider framework.
- (constraint) Use Xcode 26.6, Swift 6.3.3, native SwiftUI with thin MVVM and explicit state machines, and system SQLite through exactly pinned GRDB 7.11.1.
- (constraint) Every user or approved-AI mutation is a stable-ID append-only revision atomically committed with durable outbox and invalidations; UI success follows durable commit and device time never decides truth.
- (constraint) iPhone owns Plans, long-term records, complete conflict resolution, Analysis, AI, Health, and exchange; the chosen active capture device owns the live session, and its immutable Plan snapshot never changes after Start.
- (constraint) Important iPhone-Watch transfer is acknowledged and causal; only a compatible whole-object applied acknowledgement permits activation or cleanup, and retries are idempotent.
- (constraint) Manual local capture survives AI, HealthKit, Watch delivery, network, or optional automation failure; unsupported and uncertain paths fail closed to manual or pending states.
- (constraint) Alpha Watch automation evidence starts with Series 11 46 mm, left wrist, Crown on screen right, right-hand dominant, and the eight architecture-listed v1 candidate Exercises; broader support requires independent physical evidence.
- (constraint) Exercise, rep, load, and RPE confirmations share at most five proactive prompts in a representative approximately 20-set session; safe-rest rules govern prompting.
- (constraint) Forgotten Finish requires 15 sustained minutes of both recovery and no strength activity, suppresses on missing signals, never auto-finishes, and cools down 15 minutes after Continue.
- (constraint) Built-in Alpha AI is DeepSeek through a gated FitnessAI gateway; BYOK supports DeepSeek only with a device-local non-synchronizing Keychain secret and direct provider calls; arbitrary provider URLs are excluded.
- (constraint) Each remote request consumes a one-shot authorization bound to route, provider, account, purpose, categories and window, outbound digest, policy version, and expiry; HealthKit permission and proposal approval cannot substitute.
- (constraint) FitnessAI local records remain authoritative over HealthKit; remote Health input is selected local summaries only, and all real Health-summary transfer remains disabled until the documented provider, legal, privacy, region, retention, training-use, deletion, revocation, and user-rights gate passes.
- (constraint) Plan exchange is one plain uncompressed UTF-8 JSON Plan with the AD-10 size, depth, count, note, mapping, metadata, and draft-only limits; executable content, external references, attachments, partial import, merge, overwrite, and activation are forbidden.
- (constraint) Stores, sidecars, outbox, and chunks use completeUntilFirstUserAuthentication and are excluded from iCloud and ordinary backup; secrets use device-local non-synchronizing Keychain; unreadable stores are never replaced empty.
- (constraint) Titanium Measure semantic tokens and native platform behavior are presentation authority; top-level iPhone navigation is Plan, History, Analysis, and More with Plan as root, and Watch interaction remains shallow and task-specific.
- (constraint) Release evidence includes compile, domain, schema, safety, persistence, sync, AI-contract, secret, migration, fault-injection, accessibility, paired-device, and required physical-device tests; private-Alpha telemetry is off unless explicitly opted in.
- (constraint) Size gates remain iPhone thinned installed variant at most 500 MB, Watch uncompressed below 75 MB with installed target at most 25 MB, all Watch ML assets at most 6 MB, and RPE assets at most 2 MB.
- (decision) The first coding sequence is contract and persistence foundations, then manual iPhone session, paired Watch transfer and manual capture, AI proposal lifecycle, History and Analysis, and finally evidence-gated optional automation and external adapters; each slice must remain vertically demonstrable.
- (assumption) The missing project-context.md contains no additional binding decision because the repository-level AGENTS.md and specified planning artifacts were available and fully read.
- (question) Before the first AI Plan implementation Story, what numeric Safety Rule Pack v1 bounds and golden fixtures will the Product Owner freeze for units, contradictions, Exercise support, volume, intensity, and load progression?
- (question) Before built-in DeepSeek traffic, what current legal entity, runtime provider, processing region, subprocessors, retention/cache, model-training use, deletion/revocation, disclosure, endpoint, ceilings, rollback owner, and kill-switch record will be approved?
- (question) Before real Apple Health summary traffic, what written Apple health/fitness-purpose, DeepSeek processor/contract, privacy, cross-border, retention, deletion, revocation, and user-rights review will close the fail-closed gate?
- (question) Before automation acceptance, what preregistered frozen datasets, denominators, slice plans, confidence thresholds, RPE baseline improvement and burden limits, and false/missed reminder thresholds will be used without post-unblinding relaxation?
- (question) Before plan import acceptance, what parser time and memory budgets will supplement the already frozen AD-10 structural limits?
- (note) Wrapper-only content dropped from the contract: workflow ceremony, historical review prose, rejected vivo implementation detail beyond its non-goal, and presentation fixture values that do not change implementation decisions.
- (capability) CAP-14: Users can accept, edit, or reject next-set load suggestions and canonical substitute Exercises while planned and Actual facts remain separate; success is no mutation without user action and any future Plan change enters complete-diff approval.
- (event) Self-validation Pass 1 coherence verdict: PASS; all 14 stable capabilities have intent and testable success, constraints bend design, non-goals and concrete success signal are present, and open gaps are explicit.
- (event) Self-validation Pass 2 preservation verdict: PASS after adding CAP-14 and explicit native UI/design-system companions; all load-bearing product, UX, architecture, implementation, safety, privacy, failure, evidence, and verification claims land in the kernel or a mandatory companion.
- (event) FitnessAI implementation SPEC finalized on 2026-09-10 with bilingual artifact parity and upstream PRD/UX reconciliation complete.
