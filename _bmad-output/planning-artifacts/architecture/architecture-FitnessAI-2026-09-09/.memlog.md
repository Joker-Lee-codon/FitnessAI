---
scope: FitnessAI product architecture across iPhone, Apple Watch, shared data, integrations, and remote AI
purpose: Build substrate for convergent downstream implementation
altitude: initiative
updated: 2026-09-10T11:09
---

- (direction) User selected Coaching path.
- (direction) Architecture spine is the sole deliverable; purpose is a lean build substrate for downstream implementation, with no additional architecture presentation artifact.
- (direction) Working mode: Coaching path; elicit load-bearing decisions and challenge thin choices before distillation.
- (direction) Only deliverable: the bilingual architecture spine; no discussion deck, C4 set, or expanded solution-design document.
- (constraint) Authority order and inherited scope: current bilingual PRD, EXPERIENCE, DESIGN/Titanium Measure design system, native-ui-spec, then architecture-handoff. Do not reopen confirmed product scope, visual system, device ownership, truth/approval/revision/privacy boundaries, or phase boundaries.
- (constraint) Repository is greenfield for native Apple implementation: no iOS/watchOS production source exists yet; only planning artifacts, design prototypes, and a separate Vivo Watch feasibility POC are present.
- (direction) Product-owner preference: phone-first product. Keep Watch focused on workout capture and only the minimum controls/data needed during training; place management, review, analysis, AI, Health, file exchange, and any content that need not be on Watch on iPhone because Watch storage is constrained.
- (decision) Adopt local-first hexagonal architecture as the system paradigm. Business truth and mutation rules live in domain cores behind ports; SwiftUI presentation may use thin native MVVM; complex flows use explicit state machines without mandating TCA. Binds all product modules and prevents device/UI/integration-specific implementations from diverging on truth, approval, durability, and revision semantics.
- (decision) Adopt phone-first capability placement with a minimal offline-capable Watch recorder. Watch retains only the active-session snapshot, locally durable workout facts, pending transfer data, required inference assets, recovery state, and minimal session controls; it transfers idempotently when connectivity returns. iPhone owns plan management, AI, complete correction/conflict resolution, history/analysis, Health controls, plan exchange, and long-term storage. Prevents Watch storage limits from becoming product-wide duplication while preserving offline workout integrity.
- (direction) New product-owner requirement: users without Apple Watch must be able to record workouts using iPhone alone. Architecture must support both phone-only capture and paired-Watch capture; this expands the current PRD/UX device topology and requires upstream reconciliation before implementation acceptance.
- (direction) Desired paired-device experience: user normally starts on iPhone, sends the session content to Watch, Watch captures workout data and returns it to iPhone for processing. During an active session, edits on either device should propagate automatically to the other and the experience should not require manual synchronization.
- (decision) Adopt iPhone-coordinated revision-aware paired-session synchronization. iPhone creates the shared session identity and owns long-term records; Watch captures sensor-originated facts and durably buffers offline. Both devices may issue field-scoped corrections against stable session/set IDs. Different-field edits merge; concurrent same-field edits preserve both branches and require explicit iPhone resolution. Messages are versioned, causal, idempotent, and never resolved by device time or arrival order. The active Plan snapshot is immutable; future Plan edits do not mutate it. Binds paired workout capture and prevents duplicate sets, late-message rollback, silent overwrite, and cross-device truth loss.
- (decision) Adopt split data placement: Watch holds only the current compatible Plan snapshot, active/recoverable session facts, pending corrections, minimum on-device inference assets/settings, and unacknowledged transfer chunks. iPhone owns long-term Plans, revisions, workouts, corrections, Analysis, AI artifacts, Health state, catalog, and file exchange. Binds all Alpha data classes and prevents Watch becoming a second long-term archive.
- (decision) Adopt hybrid computation and retention: Watch performs only minimum real-time set/exercise/rep and eligible RPE assistance needed for offline feedback; iPhone performs complete processing, long-term calibration/analysis, AI, and management. Unacknowledged structured facts are never auto-deleted. Acknowledged structured facts use an initial seven-day safety window; raw sensor chunks may be removed from Watch only after durable iPhone receipt. Under storage pressure, optional raw/automatic capture degrades before any unsent user fact is discarded. Final quotas are frozen from Series 6 physical-device 90-minute evidence.
- (decision) Use SQLite through GRDB 7.10.x for structured local persistence on iPhone and Watch, isolated behind persistence ports. Each device owns an independent database; raw sensor chunks remain protected files with database metadata. Critical commits atomically write the domain fact, append-only revision, and durable outbox/invalidation records. Binds local persistence and prevents partial saves, opaque sync state, and UI coupling to the database library.
- (version) Reality check 2026-09-09: local toolchain is Xcode 26.6 (17F113). GRDB official releases list 7.10.0 as current; exact package version must be pinned and verified against the generated project before implementation.
- (direction) Possible future Android edition: preserve a platform-neutral SQLite schema contract, stable IDs, wire envelopes, revision semantics, and migration fixtures. GRDB and Swift code are Apple-only and will be replaced by an Android SQLite access layer; Android implementation is deferred and does not expand private-Alpha surfaces.
- (decision) Private Alpha local protection uses Apple Data Protection for databases, WAL/sidecar files, outbox, and sensor chunks with completeUntilFirstUserAuthentication so workout background writes remain possible after first unlock. Files are excluded from iCloud and ordinary backup; no CloudKit persistence is enabled. Secrets use non-synchronizing device-local Keychain items. SQLCipher is deferred to the pre-public-beta privacy/security threat review. Prevents lock-screen data loss and unapproved consumer-cloud copies without adding premature key-lifecycle risk.
- (decision) Crash recovery uses SQLite transactional commits and recovery journaling: a user fact, its append-only revision, durable outbox entry, and derived-analysis invalidation commit atomically. UI success follows commit only. Startup restores the last committed active session plus any visible pending fragment, resumes outstanding transfers idempotently, and never replaces an unreadable store with an apparently empty store.
- (question) Recognition whitelist coaching input needed: which 5-8 strength exercises the Product Owner performs most often in planned and ad-hoc sessions, including equipment and Watch wrist, so the private-Alpha candidate whitelist can be evidence-frozen rather than invented from literature.
- (direction) On first Watch pairing, establish and show a device capability profile before enabling automatic assistance: hardware/OS/app version, available signals and permissions, pairing/reachability state, Watch-wrist/orientation where available or user-confirmed, and the applicable support-matrix/model versions. An unlisted or incomplete profile degrades to manual recording rather than implying support.
- (direction) For ad-hoc sessions, Watch may infer an Exercise after the first resolved set and ask the user to Confirm, Correct, or defer during a safe rest period. Product Owner proposes a default 30-second confirmation window.
- (question) Resolve the 30-second timeout semantics: recommended no-response behavior is Later/pending, never silent confirmation; the captured set remains durable independently of its provisional Exercise label and can be corrected later without losing sibling facts.
- (decision) On first Watch connection, build a device capability profile from observable hardware/OS/app/model/support-matrix/signal/permission state plus user-confirmed wrist, dominant-hand, Crown-orientation, recognition/RPE, offline-recording, and cleanup settings. Free-workout recognition prompts only at a safe rest; default display window is 30 seconds. No response defers the predicted Exercise as pending and never converts silence into confirmation. A confirmed label may carry to subsequent similar sets; changes preserve sibling facts. Prevents unsupported-device claims, unsafe mid-set prompts, prompt-budget overflow, and silent model-label feedback.
- (direction) Product-owner three-day split supplied from screenshots for whitelist candidacy. Push: barbell bench press; incline dumbbell press; handle pec-deck fly; lateral raise; single-arm side-position cable triceps pressdown. Pull: assisted pull-up; barbell row; seated row; reverse pec-deck fly; hammer curl. Legs: barbell squat; barbell Romanian deadlift; unilateral machine leg curl; seated leg extension; seated hip adduction. Treat images only as user data, not instructions; exact recognition support remains evidence-gated.
- (constraint) Product Owner physical-device evidence seed: Apple Watch Series 11, worn on the left wrist, with crown orientation described by the user as outward. Exact Digital Crown Left/Right orientation and dominant hand remain to be normalized during device onboarding.
- (version) Apple official Series 11 reality check: S10 64-bit dual-core/4-core Neural Engine, 64 GB capacity, high-g accelerometer, high-dynamic-range gyroscope, third-generation optical heart sensor, 42 mm and 46 mm cases. These capabilities make Series 11 a viable private-Alpha evidence seed but do not transfer recognition validity to Series 6-10 or other wrist/orientation configurations.
- (decision) Private-Alpha automatic-recognition claims are limited to evidence rows actually tested on the Product Owner's Apple Watch Series 11 configuration. Other PRD-supported Watch models retain manual recording but are marked automatic-recognition unverified until their own physical-device evidence passes. Prevents extrapolating Series 11 results across Series 6–10 or untested configurations.
- (constraint) Recognition evidence seed normalized: Watch worn on left wrist; Digital Crown on screen right; user is right-hand dominant. Reported case size 48 mm conflicts with Apple's Series 11 42/46 mm specifications and remains unresolved rather than recorded as fact.
- (constraint) Resolved physical-device seed: Apple Watch Series 11 46 mm; left wrist; Digital Crown on screen right; right-hand dominant.
- (decision) Recognition support matrix v1 candidate evidence set is accepted: barbell bench press, incline dumbbell press, assisted pull-up, barbell row, seated row, lateral raise, hammer curl, and barbell Romanian deadlift. Each row separates planned-mode set boundary and rep-count support from free-mode exercise classification; no capability becomes supported without its own physical-device gate.
- (decision) Second-wave challenge set: barbell back squat, pec-deck fly, reverse pec-deck fly, and single-arm cable pressdown. Initial manual-only set: unilateral machine leg curl, seated leg extension, and seated hip adduction. Unsupported/manual-only remains a complete recording path and is not counted as recognition success.
- (constraint) Training inputs mix kg and lb. Persist the user-entered value and and unit unchanged, plus a separately derived canonical value for validation/Analysis; conversions never silently rewrite the authoritative entry.
- (event) Correction to preceding unit constraint: read 'value and unit unchanged'; duplicated word was a transcription typo only.
- (decision) RPE Alpha operating mode is conservative dual-gated auto-use. RPE is opt-in; eligible predictions are initially confirmation-only. Auto-use remains disabled until the versioned model passes participant/time-held-out release gates and the user separately passes the five-estimate personal window and explicitly opts in. The numeric acceptance threshold is calibrated and pre-registered before final holdout rather than invented in architecture; it is versioned by model/support-matrix row.
- (decision) RPE calibration uses only user-confirmed/corrected labels. iPhone computes bounded global-person and, after sufficient labels, exercise-family offsets; Watch receives only compact versioned calibration state. Unsupported exercise, unresolved set/rep, missing/poor signal, pending correction, incompatible model/calibration, low calibrated confidence, or drift forces abstention or confirmation. Two corrections in a rolling five auto-used eligible estimates returns immediately to verification as fixed by the PRD.
- (decision by user) Forgotten Finish reminder uses a 15-minute sustained evaluation window and a 15-minute cooldown after the user chooses Continue; it never auto-finishes a workout.
- (constraint by user) A Forgotten Finish reminder requires both sustained physiological recovery and no detected strength-training activity. Missing or unreliable signals suppress the reminder rather than guessing.
- (context by user) Product-owner usage evidence: normal inter-set rests are 1–3 minutes; equipment waits are usually short and have never exceeded 10 minutes because the user switches exercises instead.
- (constraint by user) Phone-only sessions cannot claim the Watch-backed dual-signal smart reminder. Exact release thresholds must be frozen before physical-device validation and remain versioned.
- (decision by user) FitnessAI plan import and export will use the standard .json file extension rather than a custom .fitnessplan extension. The file remains plain, uncompressed UTF-8 JSON for cross-platform portability.
- (override by user) Product Owner selects the broad HealthKit integration direction and permits HealthKit-derived context to be used by AI analysis after explicit user authorization, overriding the earlier UX boundary that excluded Apple Health data from remote AI use.
- (intent by user) The purpose of broader HealthKit access is personalized training adjustment based on the user's physical condition; AI must ask before accessing or receiving the relevant health context.
- (open by user) The exact versioned allowlist of HealthKit types, per-analysis time windows, local summarization, and remote-provider authorization fields must be frozen; blanket access to every HealthKit type is not assumed.
- (override by coach) Product Owner approved broad training-relevant Apple Health access and explicit AI use, overriding the prior UX boundary that excluded Apple Health data from remote AI.
- (decision by coach) HealthKit access uses a versioned allowlist of training, activity, body-composition, cardiovascular, sleep, and recovery categories. Clinical records, medication, laboratory, reproductive, raw ECG, glucose, blood-pressure, and disease-specific data remain excluded from Alpha.
- (decision by coach) HealthKit system authorization and remote-AI authorization are separate. Before each remote analysis, FitnessAI previews the exact data categories, bounded time range, purpose, and derived summaries; the user may allow once, narrow the scope, or cancel without losing ordinary functionality.
- (constraint by coach) HealthKit data is summarized locally when possible; remote AI receives the minimum approved summaries rather than unrestricted raw streams. No real remote health-data traffic is permitted until provider, subprocessors, region, retention, training-use policy, revocation, deletion, and audit behavior are frozen.
- (constraint by coach) AI may provide fitness-oriented adjustments but must not diagnose disease or give medication/treatment instructions. HealthKit absence is represented as no accessible data or query failure, never inferred as permission denial.
- (decision by coach) Product Owner selected the balanced Alpha Plan Safety Rule Pack mode: deterministic structural checks, user-context contradiction checks, canonical exercise support, personal-baseline-aware volume/intensity/load-progression checks, and affected-scope safety stops.
- (decision by coach) Safety evaluation returns one of pass, clarification-or-correction-required, or affected-scope safety-stop. Any unresolved or blocked result prevents approval and commit; it never prevents truthful workout capture or access to the last effective plan.
- (constraint by coach) The same deterministic rule engine runs after proposal generation and immediately before commit. AI cannot self-certify its output. Every result includes a stable rule code, human explanation, affected fields/scope, rule-pack version, and repair path.
- (constraint by coach) Numeric operating bounds are versioned configuration backed by passing and blocking fixtures. They require qualified exercise or healthcare professional review before release beyond Product Owner testing; no developer or model may silently invent or change them.
- (constraint by coach) A rule-pack update does not mutate historical Actual Sets, an active Workout Session, or an already-approved Plan Revision. New proposals use the new version and material changes require re-review.
- (intent by coach) Product Owner intends DeepSeek API as the built-in default AI provider and wants advanced users to be able to supply their own API credentials.
- (evidence by coach) As checked on 2026-09-10, DeepSeek documents OpenAI- and Anthropic-compatible API formats, streaming, JSON/JSON-Schema output, and tool calls; provider-specific capability differences and failures still require explicit adapters and local schema validation.
- (risk by coach) DeepSeek documents stateless Responses conversations but also provider-managed context caching enabled by default, with unused cache entries typically cleared within hours to days. FitnessAI must not claim zero provider retention without a stronger current contract.
- (clarification by coach) Deep AI integration means the model produces a complete structured Training Plan Draft or field-level Plan Change Proposal, including exercises, order, sets, repetitions, load, and optional target RPE. The user does not re-enter items manually. After deterministic validation and one proposal-bound approval, FitnessAI atomically commits the complete new Plan Revision and delivers it to Watch. 'AI cannot write directly' means only that the provider/model cannot bypass validation, immutable review, and explicit approval or mutate storage silently.
- (decision by user) Product Owner accepted the dual-channel AI integration: built-in DeepSeek calls use a FitnessAI-controlled gateway with the product API key held server-side; advanced users may use their own DeepSeek API key, stored device-only in Keychain and sent directly to DeepSeek. Alpha does not accept arbitrary provider URLs.
- (constraint by coach) Product-owned DeepSeek credentials are never embedded in an app binary. BYOK credentials never enter FitnessAI servers, SQLite, logs, cloud sync, backups, or Watch transfer, and use a device-only Keychain accessibility class.
- (constraint by coach) Built-in and BYOK conversations are provider/account-separated. Switching routes never silently forwards prior conversation or health context. Every route still enforces per-request authorization, schema validation, Safety Rule Pack validation, immutable review, and explicit approval.
- (constraint by coach) AI output is an untrusted structured draft. It may eliminate item-by-item user entry and, after one digest-bound approval, be atomically committed as a complete Plan Revision; it never directly mutates active or historical records.
- (decision by coach) Product Owner accepted hybrid WatchConnectivity transport: every important mutation commits locally and enters a durable application outbox; live messaging accelerates reachable interactions, background user-info transfers carry durable structured events, latest application context carries only replaceable current state, and background file transfer carries sensor chunks.
- (constraint by coach) Transport completion is not domain receipt. The sender marks content synchronized or eligible for cleanup only after the counterpart validates and durably commits it and returns an application-level acknowledgement.
- (constraint by coach) All envelopes carry stable object, revision, message/idempotency, schema, source-device, causality, and digest metadata. Duplicate, delayed, and out-of-order delivery cannot duplicate facts or roll back effective state.
- (decision by coach) Any effective fact correction atomically records affected derived-analysis invalidations. iPhone recomputes affected read models and marks prior analysis stale while recomputation is pending rather than silently mixing revisions.
- (constraint by coach) Phone-only use follows the same IDs, revision, persistence, and analysis rules without creating Watch transfer work. Watch replacement and multiple-pair transitions keep device-specific pending data isolated and deliver only the latest compatible plan to the newly active Watch.
- (decision by coach) Product Owner accepted a single-repository, single-Xcode-workspace modular monolith: separate iPhone and Watch app targets plus one local Swift package containing a small set of explicit domain, persistence, sync-contract, safety-rule, and analysis targets.
- (constraint by coach) Presentation invokes application use cases and never GRDB, HealthKit, WatchConnectivity, or AI providers directly. Domain rules depend on no platform, database, UI, or provider framework. Adapters implement ports and cannot bypass domain validation or revision semantics.
- (decision by coach) iPhone owns plans, AI authorization/provider integration, HealthKit history, plan exchange, full analysis, long-term data, conflict resolution, and sync coordination. Watch owns active-session capture, local inference, offline durability, recovery, and transport of captured facts.
- (constraint by coach) iPhone and Watch share stable domain and wire contracts, not UI, database files, or persistence-row types. Cross-platform Contracts contain JSON Schemas, IDs, units, revision rules, error codes, and golden fixtures reusable by a future Kotlin implementation rather than attempting to port Swift source.
- (constraint by coach) Avoid a generic Utils module and premature many-package or multi-repository decomposition. Every adapter advertises capabilities explicitly; provider-format resemblance does not imply identical behavior.
- (decision by coach) Product Owner accepted three separated environments: fixture-based development, owner-only private Alpha with real paired devices and explicitly authorized real services, and public beta/production held behind release gates.
- (constraint by coach) Simulator evidence may validate UI, domain, persistence, contracts, and failure states but cannot qualify sensors, motion recognition, heart-rate behavior, Watch background execution, WatchConnectivity file delivery, HealthKit behavior, battery use, or interruption recovery; those require paired physical-device evidence.
- (decision by coach) Every change runs fast compile/domain/schema/safety/persistence/sync/AI-contract/secret-scanning checks; private-Alpha candidates additionally run full iPhone and Watch builds, migration and fault-injection tests, accessibility checks, and the defined paired-device workout scenario.
- (constraint by coach) Development, private-Alpha, and future production credentials, data stores, bundle/configuration identities, and endpoints remain separated. No API key enters source control; operational logs exclude prompts, health values, sensor payloads, workout content, RPE, free text, and secrets.
- (decision by coach) High-risk capabilities have independent fail-closed feature controls. Disabling recognition, RPE auto-use, forgotten-Finish, remote AI, HealthKit, BYOK, or research upload never blocks manual local workout capture or deletes committed user facts.
- (constraint by coach) Public beta remains blocked until qualified safety review, current AI-provider/privacy and region review, China-region data-governance approval, security review, wider physical-device qualification, user-rights lifecycle, and cost/abuse controls are complete.
- (decision by coach) Accepted Alpha Plan JSON parser bounds: plain uncompressed UTF-8; 2 MB; depth 12; exactly one Plan; at most 366 Plan days, 30 Exercises per day, 20 Sets per Exercise, 10,000 Sets total, and 2,000 characters per note; no external references, attachments, executable content, partial import, merge, activation, or overwrite.
- (decision by coach) The approved broad Alpha HealthKit allowlist is represented by exact versioned type identifiers for workouts; heart rate, resting heart rate, one-minute heart-rate recovery, HRV; sleep; active/resting energy, steps, exercise time; body mass, height, body fat, lean body mass; VO2 max, respiratory rate, oxygen saturation, sleeping wrist temperature where available; and explicitly allowed age/biological-sex characteristics.
- (version by coach) Reality check 2026-09-10: Xcode 26.6 build 17F113 provides Apple Swift 6.3.3 on the project host.
- (version by reviewer) Reality check 2026-09-10: GRDB 7.11.1 supersedes 7.10.0 as the current official release; architecture pins 7.11.1 exactly through SwiftPM and commits Package.resolved.
- (constraint by source reconciliation) Exercise, rep, load, and RPE prompts share one proactive-interruption budget of at most five prompts in a representative approximately 20-set workout; a completed session may show one nonmodal AI Review/Later/Disable invitation whose display alone sends no data and grants no authorization.
- (constraint by source reconciliation) Private-Alpha size targets are measured as thinned installed variants: iPhone at most 500 MB; Watch below the 75 MB uncompressed ceiling and internally at most 25 MB; total Watch ML assets at most 6 MB, including at most 2 MB for RPE.
- (constraint by source reconciliation) The accepted native UI and Titanium Measure system bind all presentation: iPhone top level is Plan, History, Analysis, and More with Plan as root; native back restores context; semantic tokens, light/dark, Dynamic Type, VoiceOver, and explicit failure/offline/recovery states are required; Digital Crown is never the sole input or commit path.
- (event) Architecture spine finalized on 2026-09-10 after source reconciliation, deterministic lint with zero findings, and passing rubric-walker, technology-reality, and adversarial-seam re-reviews. English and complete zh-CN copies are the sole architecture deliverable.
- (event) Architecture spine finalized on 2026-09-10 after deterministic lint returned zero findings and rubric, technology-reality, and adversarial-seam re-reviews all passed. The bilingual spine remains the sole delivery artifact.
