# Editorial Review — Prose

| Original Text | Revised Text | Changes |
|---------------|--------------|---------|
| “the starting data boundary is FitnessAI local structured user-confirmed or corrected workout records” (`addendum.md`, Deferred Input and Training Plan Exchange Boundaries); “eligible local records mean FitnessAI local structured user-confirmed or corrected workout records” (`prd.md`, DV-006) | “the starting data boundary comprises structured FitnessAI workout records that are stored locally and confirmed or corrected by the user”; “eligible local records are structured FitnessAI workout records that are stored locally and confirmed or corrected by the user” | Clarifies which modifiers apply to the records and removes the difficult noun stack without changing the accepted data boundary. |
| “Parsed results remain provenance-bearing, correctable drafts until the applicable confidence and conflict rules permit explicit commit.” (`prd.md`, §6.3) | “Parsed results remain provenance-bearing, correctable drafts until the user explicitly confirms or corrects them under the applicable conflict rules.” | Removes an implication that confidence alone can authorize commit; aligns the summary with FR-074’s explicit-confirmation contract without changing scope or stage. |
| “FitnessAI keeps target, predicted, individually confirmed, auto-accepted, corrected, and effective Actual RPE states distinguishable.” (FR-026); “distinguish user-confirmed or auto-accepted RPE provenance” (FR-040) | “FitnessAI keeps target, predicted, individually confirmed, auto-used, corrected, and effective Actual RPE states distinguishable.”; “distinguish user-confirmed or auto-used RPE provenance” | Uses the defined conceptual term “auto-used” consistently with the glossary, MVP scope, and NFR-RPE-009/013. The technical field name `autoAcceptedPredictedRPE` can remain unchanged. |
| “Voice input requires observable permission, listening, transcription, draft, confirmation, commit, cancellation, interruption, and unavailable states.” (NFR-VOICE-001) | “Voice input requires observable states for permission, listening, transcription, draft review, confirmation, commit, cancellation, interruption, and unavailability.” | Makes “states” govern the entire list and replaces the adjective “unavailable” with the parallel noun “unavailability.” |
| “When voice input is unavailable, interrupted, ambiguous, or conflicts with a touch, sensor, or synchronized revision” (FR-075) | “When voice input is unavailable, interrupted, ambiguous, or conflicts with a touch-entered value, sensor-derived value, or synchronized revision” | Names comparable conflict objects; “touch” and “sensor” alone are input mechanisms, not values that can conflict with a revision. |

