| Original Text | Revised Text | Changes |
|---------------|--------------|---------|
| Its primary promise is to create a complete, truthful workout record with near-zero in-workout interaction, so trainees can stay focused instead of repeatedly switching between Watch and phone or rebuilding missing data after the session. | Its primary promise is to create a complete, truthful workout record with almost no interaction during the workout, so trainees can stay focused instead of repeatedly switching between Watch and phone or rebuilding missing data after the session. | Replaced the compressed phrase “near-zero in-workout interaction” with a more natural construction so the core promise is immediately clear (§1). |
| **FR-025:** Low-confidence, unfamiliar, missing-signal, or drifting RPE cases remain pending or request bounded confirmation rather than fabricating an Actual RPE. | **FR-025:** Low-confidence, unfamiliar, missing-signal, or drifting RPE cases remain pending or trigger a confirmation prompt within the interruption budget rather than fabricating an Actual RPE. | Clarified that the system, not an RPE case, triggers the prompt and made “bounded” explicit by tying it to the defined interruption budget (§7.3). |
| **FR-027:** Based on the Actual RPE and remaining Training Plan, FitnessAI may suggest a next-set load; the user explicitly accepts, rejects, or edits the suggestion before it changes the next Actual Set. | **FR-027:** Based on the Actual RPE and remaining Training Plan, FitnessAI may suggest a next-set load. The suggestion may change the next Actual Set only after the user explicitly accepts or edits it; the user can also explicitly reject it. | Removed the ambiguous pronoun reference and clarified that rejection does not change the next Actual Set (§7.3). |
| Conflicts involving field corrections, split or merged sets, deletion, completion state, or Training Plan revisions preserve every user-authored revision until an effective revision is determined. | FitnessAI preserves every user-authored revision involved in conflicts over field corrections, split or merged sets, deletion, completion state, or Training Plan revisions until an effective revision is determined. | Replaced the illogical subject “Conflicts” with the responsible system and clarified what the conflicts concern (FR-046, §7.6). |
| **FR-068:** Cross-device restoration reports whether required account-scoped records are complete, still synchronizing, or failed. | **FR-068:** Cross-device restoration reports whether restoration of the required account-scoped records is complete, still in progress, or has failed. | Made the three restoration states grammatically parallel and attached them to the restoration process rather than to the records (§7.10). |
| **NFR-RPE-004:** Before user-facing release, the estimator must demonstrate participant-held-out and later-session improvement over simple baselines such as target RPE, previous-set RPE, and personal Exercise history. | **NFR-RPE-004:** Before user-facing release, the estimator must demonstrate improvement over simple baselines such as target RPE, previous-set RPE, and personal Exercise history on participant-held-out data and in later sessions. | Clarified that “participant-held-out” and “later-session” identify evaluation contexts, not types of improvement (§8.2). |
