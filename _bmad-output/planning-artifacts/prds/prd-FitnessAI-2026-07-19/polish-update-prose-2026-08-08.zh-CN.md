# 编辑审阅——措辞

| 原文 | 修订文本 | 变更说明 |
|------|----------|----------|
| “the starting data boundary is FitnessAI local structured user-confirmed or corrected workout records”（`addendum.md`，Deferred Input and Training Plan Exchange Boundaries）；“eligible local records mean FitnessAI local structured user-confirmed or corrected workout records”（`prd.md`，DV-006） | “the starting data boundary comprises structured FitnessAI workout records that are stored locally and confirmed or corrected by the user”；“eligible local records are structured FitnessAI workout records that are stored locally and confirmed or corrected by the user” | 明确各修饰语与 records 的关系，消除难以解析的名词堆叠，同时不改变已确认的数据边界。 |
| “Parsed results remain provenance-bearing, correctable drafts until the applicable confidence and conflict rules permit explicit commit.”（`prd.md`，§6.3） | “Parsed results remain provenance-bearing, correctable drafts until the user explicitly confirms or corrects them under the applicable conflict rules.” | 消除“仅凭置信度即可授权提交”的暗示；在不改变范围或阶段的前提下，使摘要与 FR-074 的显式确认约束一致。 |
| “FitnessAI keeps target, predicted, individually confirmed, auto-accepted, corrected, and effective Actual RPE states distinguishable.”（FR-026）；“distinguish user-confirmed or auto-accepted RPE provenance”（FR-040） | “FitnessAI keeps target, predicted, individually confirmed, auto-used, corrected, and effective Actual RPE states distinguishable.”；“distinguish user-confirmed or auto-used RPE provenance” | 与术语表、MVP 范围及 NFR-RPE-009/013 保持一致，统一采用已定义的概念术语 “auto-used”。技术字段名 `autoAcceptedPredictedRPE` 可保持不变。 |
| “Voice input requires observable permission, listening, transcription, draft, confirmation, commit, cancellation, interruption, and unavailable states.”（NFR-VOICE-001） | “Voice input requires observable states for permission, listening, transcription, draft review, confirmation, commit, cancellation, interruption, and unavailability.” | 让 “states” 明确统领整组枚举，并将形容词 “unavailable” 改为与其他项平行的名词 “unavailability”。 |
| “When voice input is unavailable, interrupted, ambiguous, or conflicts with a touch, sensor, or synchronized revision”（FR-075） | “When voice input is unavailable, interrupted, ambiguous, or conflicts with a touch-entered value, sensor-derived value, or synchronized revision” | 明确可比较的冲突对象；“touch” 和 “sensor” 本身是输入机制，而不是可与 revision 发生冲突的值。 |
